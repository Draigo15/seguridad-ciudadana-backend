# Backend .env example for SendGrid Email OTP

# Required: Firebase service account JSON encoded in Base64
# Generate: base64 -w0 service-account.json (Linux/macOS) or use an online encoder
FIREBASE_CONFIG_BASE64=BASE64_ENCODED_SERVICE_ACCOUNT_JSON

# Session token TTL (minutes)
SESSION_TTL_MINUTES=120

# Email via HTTP provider (recommended on Render)
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=SG.YOUR_SENDGRID_API_KEY

# Email sender address (must be verified in SendGrid)
SMTP_FROM=no-reply@your-domain.com

# Optional: local run port (Render assigns automatically in production)
PORT=3000